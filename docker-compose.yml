services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      DB_PATH: /app/data/tinshed.db
    volumes:
      - roster-data:/app/data
    restart: unless-stopped

volumes:
  roster-data:
